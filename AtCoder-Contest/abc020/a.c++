# include <bits/stdc++.h>

# define rep(i,n) for(int i=0;i<(n);++i)
# define rep1(i,n) for(int i=1;i<=(n);++i)
# define ALL(x) (x).begin(),(x).end()
# define pb push_back

using namespace std;
using ll = long long;

int main(){
    string one = "ABC";
    string two = "chokudai";
    int q;
    cin >> q;
    if(q==1) cout << one << endl;
    else cout<< two << endl;
}